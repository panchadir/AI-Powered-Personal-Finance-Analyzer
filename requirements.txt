# AI-Powered Personal Finance Analyzer — Phase 1 MVP
# Versions pinned to what is actually installed (Story 1.1 AC #3). Python 3.11+.
# Note: sqlmodel ships bundled with reflex. Keep this file lean: only dependencies
# actually used.

# --- Web framework + auth ---
reflex==0.9.6.post1
reflex-local-auth==0.5.0

# --- Charts (Story 5.4) — rx.plotly wraps plotly.js on the frontend, but
# `dashboard_state.py` builds the Figure objects server-side and needs the package. ---
plotly==6.9.0

# --- Statement ingestion (Epic 2) ---
pdfplumber==0.11.10
camelot-py==2.0.0
statementsparser==0.1.0
pandas==3.0.3

# --- LLM (Epics 3, 5, 6, 7) ---
anthropic==0.116.0

# --- Database driver ---
psycopg2-binary==2.9.10

# --- Config / secrets ---
python-dotenv==1.2.2

# --- Testing ---
pytest==9.1.1

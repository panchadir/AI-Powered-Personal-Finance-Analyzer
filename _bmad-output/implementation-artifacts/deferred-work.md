# Deferred Work Ledger

Items surfaced during reviews that are real but intentionally not actioned now.

## Deferred from: code review of story-7.4 (2026-07-12)

- **`scrollIntoView` hydration race on `/insights?highlight={id}`.** `rx.call_script` is returned from `InsightsState.load_insights` (the `on_load` handler) and may fire before Reflex has resolved `rx.foreach` Var bindings — `document.getElementById('insight-5')` returns `null` and `?.scrollIntoView(...)` silently no-ops. Result: user arrives from the Dashboard teaser but the linked insight is not scrolled into view. The `?.` guard already prevents any crash (AC4 satisfied). Fix requires a Reflex lifecycle hook fired post-hydration — outside this story's scope and the current Reflex 0.9.6 API. No user-visible error; scroll simply doesn't happen on that race path.

## Deferred from: code review of story-8.4 (2026-07-12)

- **ScoreEvent rows not seeded by `scripts/seed_demo.py`.** The epic AC mentions "confidence-score events seeded for the canonical demo user." The story spec explicitly notes "No income_sources table — income inferred from transactions" and scopes the seed to transactions + commitments only. ScoreEvent seed omitted by design. Revisit for Story 8.5 demo dry run if the Confidence Score card shows misleading data without seed events.
- **`scripts/demo_setup.sh` alternative not created.** The epic AC listed `make demo` or `./scripts/demo_setup.sh` as the setup command. The story implementation chose the Makefile path only. A shell-script alternative would be needed for environments without GNU Make (Windows cmd, some CI images). Defer until there is a concrete Windows or CI need.

## Deferred from: code review of story-7.3 (2026-07-11)

- **Concurrent `refresh_insights` calls for the same user (two browser tabs, rapid reloads) can both read "no active row" for the same `dedup_key` before either commits, producing duplicate active insights for one pattern (TOCTOU race).** [finance_app/state/insights_bridge.py] Not actioned: single-user-per-account MVP, no concurrent-session use case in any current story. Proper fix needs either a unique constraint on `(user_id, dedup_key)` scoped to `status='active'` (partial/conditional — dismissed rows must not be constrained) or a serialized check+insert. Revisit for Epic 8 hardening if it ever becomes reachable.
- **Each detector candidate in `refresh_insights`'s loop is read/written and `session.commit()`'d independently, not as one all-or-nothing transaction.** [finance_app/state/insights_bridge.py:202-272] A DB failure partway through leaves earlier candidates' writes already persisted with no rollback of the batch. Not actioned: matches `dashboard_state.py`'s existing error-containment risk profile elsewhere in the app (not a regression introduced by this story); the failure mode is "some insights updated, none lost or corrupted," not a data-integrity risk.
- **A pattern that stops firing (no candidate for a `dedup_key` that has an active row) is never auto-expired or auto-resolved** — the stale active insight persists indefinitely, describing behavior that may no longer be true. Not actioned: nothing in Story 7.3's ACs asks for this (Task 4's own note: "this story does not auto-expire/auto-dismiss them"). Worth a future story if demo/user feedback flags stale insights as confusing.
- **`_headline_metric` silently returns `None` on an unrecognized pattern name or a metrics-dict type/key mismatch, with no logging.** [finance_app/state/insights_bridge.py:129] Functionally safe today (the `both_unknown` guards added in this review handle it correctly on both the active-row and dismissed-row branches), but a real detector/narrator-side data-shape bug would be invisible in production. Low-value observability nice-to-have (a `log.debug`/`log.warning` call), not actioned now.

## Discovered AND fixed while implementing Story 7.3 (2026-07-11)

- ~~**The dev Docker container's `/app` source tree was silently out of sync with the local repo — missing `finance_app/state/{dashboard_state,engine_bridge,commitments_state}.py`, `finance_app/pages/commitments.py`, `services/categorize/rules.py`, and 3 test files (`tests/test_engine_bridge.py`, `tests/test_commitment_validation.py`, `tests/narrate/__init__.py`), plus a stray duplicated-nesting artifact under `services/engine/insights/insights/` and `tests/engine/insights/insights/` from an earlier `docker cp` into an already-existing directory.**~~ **✅ FIXED 2026-07-11.** Every `pytest`/`reflex compile` run earlier in this session (Stories 7.1/7.2 and the start of 7.3) was executed against this partially-synced container — the reported "all green" counts were real for the files that *were* present, but silently excluded whatever wasn't copied yet (an `ImportError` at collection would have surfaced immediately if the missing file were ever imported by something already in the container; the specific gap only became visible once Story 7.3's `insights_bridge.py` was refactored to import `engine_bridge`, which itself wasn't there). **Fix:** wiped and fully re-copied `finance_app/`, `services/`, `tests/`, and `alembic/` from the local repo into the container (`rm -rf` each directory in-container first, then a single `docker cp` of the whole tree — avoids the nested-directory bug that occurs when `docker cp`-ing a directory into a path that already exists). Verified byte-identical file listings (`find ... | sort | diff`) before trusting any test result again. **Going forward in this session:** any full-suite claim is only trustworthy after this kind of listing-diff sync, not after a partial `docker cp` of only the files touched by the current story.
- ~~**`finance_app/state/copilot_state.py`'s `messages: list[dict] = []` (Epic 6, commit `4ff2886`, pre-existing and unrelated to Epic 7) makes the *entire app* fail `reflex compile`,** not just the Copilot page.~~ **✅ FIXED 2026-07-11.** Only surfaced once the container sync above let `reflex compile --dry` actually reach `pages/copilot.py` for the first time this session (the earlier "compile succeeded" result was against the desynced container, which likely couldn't even resolve the full route table). Reflex's `rx.foreach`/`.length()` need a typed Var; a bare `list[dict]` with heterogeneous value types (`role`/`content`: `str`, `trace_sources`: `list[str]`) fails with `UntypedVarError` inside `_trace_row(msg["trace_sources"])`. **Fix:** added `ChatMessageView` (`role`, `content`, `trace_sources` — a plain dataclass, mirroring `dashboard_state.py`'s existing `ScoreEventView` pattern for exactly this problem), changed `messages: list[ChatMessageView]`, updated the 3 construction sites in `copilot_state.py` and the dict-subscript reads in `copilot.py`'s `_user_bubble`/`_bot_bubble`/`_message_bubble` to attribute access. No test coverage existed for `copilot_state.py`/`copilot.py` before or after this fix (a pre-existing gap, out of this story's scope to close). Verified: `reflex compile --dry` → success; full suite still 498 passed / 6 skipped, zero regressions.

## Deferred from: code review of story-1.1 (2026-07-09)

- ~~**AD-2 boundary guard is reflex-only.** `tests/test_service_boundary.py` detects `import reflex` / `from reflex import` but not a `services/` module importing the UI layer (`from finance_app import …`), which also violates the one-way UI→Service→Data dependency direction (AD-2).~~ **✅ RESOLVED 2026-07-09** (same session, at user request): guard generalized to check top-level import roots; new `test_no_ui_layer_import_in_services` flags any `services/`→`finance_app` import. Detection verified; suite 6 passed. No longer deferred.

## Deferred from: code review of story-1.3 (2026-07-09)

- **`handle_registration` orchestration is app-verified only, not unit-tested.** The pure/DB core (`register_new_user`) is unit-tested and the `/register` route serves, but the handler's auto-login + `rx.redirect("/upload")` + `email_taken` wiring is verified by `reflex run`, not a unit test (testing `rx.State` handlers that touch `rx.session()`/`self.router` is fragile). Add a handler-level test with a mocked session once a Reflex state-test harness exists (natural fit: Epic 8 E2E).
- **Duplicate-email registration is not concurrency-safe.** `register_new_user` does check-then-insert; simultaneous same-email registrations would let the second raise an unhandled `IntegrityError`. The `unique` constraint on `localuser.username` protects data integrity (no dup row); single-user local MVP → very low severity. Phase-2 multi-user: catch `IntegrityError` and return `email_taken=True`.
- ~~**Story 1.4 login must lowercase email before lookup.** Registration normalizes email to lowercase before storing in `localuser.username`; the 1.4 login handler must apply the same normalization before its `select(...).where(username == email)` or a user who typed a mixed-case email at registration couldn't log in with the same casing.~~ **✅ RESOLVED (stale entry, verified 2026-07-10).** This was a forward-looking note for Story 1.4, and 1.4 implemented it: `authenticate()` and `handle_login()` both normalize with `(email or "").strip().lower()` before the lookup (`finance_app/state/auth_state.py`), and `tests/security/test_login.py` covers the mixed-case path. Nothing to do; closing the entry so the ledger stops advertising an open item that is not open.

## Deferred from: story-1.3 planning — AD-5 httpOnly (2026-07-09)

- **True httpOnly auth cookie (AD-5 / FR-1.1) deferred to Phase 2.** The installed `reflex-local-auth==0.5.0` stores the session token in `rx.LocalStorage`, and Reflex state tokens are inherently client-visible — a true httpOnly cookie (invisible to JS) cannot come from `rx.State`/`rx.Cookie`; it requires custom server-side `Set-Cookie` middleware. **Product decision (2026-07-09, ALPHA):** Phase 1 uses a **SameSite `rx.Cookie`** (satisfies "never localStorage/sessionStorage"; not httpOnly), justified by the desktop-only/localhost/single-user threat model. Phase 2 hardening: implement server-side httpOnly session cookies via FastAPI middleware on the Reflex backend, and flip `secure=True` under https. AD-5 should be amended (or annotated) to reflect the Phase-1 SameSite-cookie baseline vs the Phase-2 httpOnly target. Consider a `correct-course` pass to formally reconcile the AD text.
- **Vestigial `_auth_token` localStorage slot from reflex-local-auth's unused `LocalAuthState` (Story 1.3, AC #3 partial).** Our real token is a SameSite cookie owned by `finance_app.state.auth_state.AuthState` (verified at `reflex run`: compiled `clientStorage.cookies` carries `_auth_token` with `sameSite=strict`). But merely importing `reflex_local_auth` for its `LocalUser`/`LocalAuthSession` models also registers the library's `LocalAuthState`, whose `rx.LocalStorage` `auth_token` compiles into `clientStorage.local_storage` as an empty, **never-written** `_auth_token` slot (nothing calls `LocalAuthState._login`; we use our own state). Security intent is met (the real token is a cookie; the localStorage slot never holds it), but the slot exists. **Phase-2 removal:** when the httpOnly server-side auth lands, replace reflex-local-auth's state layer entirely (keep only its models or fork them) so no localStorage token var is registered at all.

## Deferred from: code review of story-1.2 (2026-07-09)

- ~~**tz-aware timestamp defaults into naive `DateTime` columns.** `finance_app/models.py:38` `_utcnow()` returns a tz-aware UTC datetime that is written into SQLAlchemy naive `DateTime` columns (`created_at`, `timestamp`, `uploaded_at` across the new tables). No consumer reads these timestamps yet, so it is harmless today. When timestamps are first consumed (Epic 4 score_events / Epic 5 dashboard freshness), standardize the convention — either naive-UTC everywhere (`datetime.now(timezone.utc).replace(tzinfo=None)`) or tz-aware columns — to avoid comparison/serialization inconsistency.~~ **✅ RESOLVED 2026-07-10** (Epic 5 — the consumer this deferral was waiting for): `models._utcnow()` now returns **naive UTC** (`datetime.now(timezone.utc).replace(tzinfo=None)`), so the write side agrees with the naive columns and a round-tripped timestamp compares cleanly against an aware `now()` without raising `TypeError`. **Convention pinned: every stored timestamp is UTC, and naive.** `engine_bridge._as_utc` remains as the single read-side lift to aware UTC (it also passes through already-aware values, so rows written before this change still render). Regression-tested in `tests/test_engine_bridge.py::TestTimestampConvention` (naive, actually-UTC-not-local, and a persisted `score_events` row round-tripping through the drill-in). No migration needed — the columns were always naive.

## Deferred from: story-1.4 implementation (2026-07-09)

- ~~**`rxconfig.py` hard-requires `DATABASE_URL` with no committed `.env` -> fresh checkout can't run tests or the app.** An uncommitted Postgres/Docker change made `rxconfig.py:10` do `db_url=os.environ["DATABASE_URL"]`; with no `.env` present, `pytest` fails at collection (`KeyError: 'DATABASE_URL'`) and `reflex run` won't start.~~ **✅ RESOLVED 2026-07-09** (spec `spec-1-4-database-url-fallback.md`, one-shot): `rxconfig.py` now resolves `db_url` via `os.environ.get("DATABASE_URL") or "<local Postgres DSN>"` (Postgres is the Phase-1 store per product directive; DSN matches `.env.example`). Blind Hunter review surfaced that `alembic/env.py:24` had the same hard-require, so the identical fallback was applied there too (the two are cross-referenced by comment). `docker-entrypoint.sh` intentionally keeps the hard-require — it only runs in-container where docker-compose always sets `DATABASE_URL`. The `or` (vs `.get` default) also covers a present-but-empty var. Verified: both files parse and resolve unset/empty→default, explicit→respected. No longer deferred.
- **Story 1.4 login/logout: live-browser DevTools round-trip not captured.** The cookie set-on-login / clear-on-logout and the on_load redirect to `/login` are proven by the successful frontend compile + unit tests on the pure decision paths, but not by a live browser session (no browser driver in the dev env). Recommend a manual browser pass at review, and fold the full register->login->cookie->guard->logout E2E into Epic 8.

## Deferred from: code review of story-1.4 (2026-07-09)

- **`rx.State` guard-glue is unit-untested (highest-value coverage gap).** `check_auth` returning `rx.redirect(LOGIN_ROUTE)`, `do_logout`'s cookie handling, `handle_login`'s `_login`+redirect, and the `on_load` guard registration on all 5 protected pages are verified only by `reflex run` — a regression that made `check_auth` return `None` unconditionally (guard disabled) would pass every unit test in the suite. Testing `rx.State` on_load/redirect handlers reliably needs a Reflex state-test harness that doesn't exist yet. Fold the full register→login→cookie→guard→logout E2E into Epic 8.
- **`on_load` route guard flashes the page before redirecting, and is not a data-access control.** The guard fires after the component mounts/serves, so an unauthenticated visitor briefly sees the protected page before the redirect round-trip. Harmless on the current `coming_soon` placeholders, but when these pages render real user data (Epic 2+) a data-loading computed var could compute for an unauthenticated token during the flash. Real pages MUST scope their own reads by `user_id` (AD-4) regardless of the guard; consider a server-side (middleware) guard alongside the Phase-2 httpOnly work so auth is enforced before render.
- **IDOR baseline covers only a single flat table.** `test_idor_baseline` exercises one `SELECT ... WHERE user_id == ?` on `transactions` — no joins, subqueries, aggregates, or the other user-scoped tables (`merchant_rules`, `score_events`, `insights`, `chat_messages`). The exhaustive cross-user sweep is Epic 8 / Story 8.3 (already the story's documented scope boundary; reaffirmed by review).

## Deferred from: one-shot spec-1-4-database-url-fallback (2026-07-09)

- ~~**`docker-entrypoint.sh` DSN parsing is fragile (pre-existing; surfaced by Blind Hunter, not caused by the fallback change).** The Postgres-wait probe parses `DATABASE_URL` with `re.match(r".*://([^:]+):([^@]+)@([^:/]+)(?::(\d+))?/(.+)", url)`; a password containing `@`/`:`/`/` mis-parses, and any URL the regex can't match makes `m` `None` → `AttributeError` instead of a clear message. Only runs in-container so severity is low today (creds are the known compose values), but harden it (parse with `sqlalchemy.engine.make_url` / `urllib.parse`, and fail with a readable message) when entrypoint work is next touched. Source: adversarial review of the `DATABASE_URL` fallback change.~~ **✅ RESOLVED 2026-07-10** (Epic 5): the regex is replaced by `sqlalchemy.engine.make_url`. Two real bugs went with it — (1) the old pattern never URL-decoded the password, so a *correctly* percent-encoded DSN authenticated with the literal `p%40ss…`; (2) an unmatchable DSN made `m` `None` → `AttributeError`. Validation is also **hoisted out of the `until` wait-loop**: a malformed DSN is a configuration error and now aborts with a readable message, where previously any non-zero exit (including the `AttributeError`) was retried forever at one second per attempt. Only genuine connectivity failures are retried.

## Deferred from: code review of Stories 2.1 & 2.2 (2026-07-10)

- ~~**CSV footer / non-transaction rows abort the whole parse (medium).** `CSVParser._row_to_txn` raises `MISSING_AMOUNT` on any content-bearing row that has neither a debit nor a credit amount (verified: a trailing `Opening Balance` / `Statement Total` row fails the entire file). Real Indian-bank CSV exports routinely append such summary/disclaimer rows. Not a Story 2.2 AC violation -- the golden fixtures (HDFC/SBI) contain no footers -- so deferred rather than patched now. The correct fix touches AD-12: **skip** clearly-non-transaction rows but **surface a skipped-row count** so the upload summary can caveat (never silently show fewer rows than the file contained). This belongs with **Story 8.1 (edge-case honest refusals)** / the upload-summary surface in Story 2.4, which own the caveat UI.~~ **✅ RESOLVED 2026-07-10** (user request, same Epic 2 branch -- fixed directly, no longer deferred to 8.1): `_row_to_txn` now returns `None` for **structural** rows -- those with no debit, no credit, AND no valid date (footer/summary rows like `Opening Balance` / `Statement Total`) -- so they are skipped while the transaction count stays accurate. The earlier worry (needing a skipped-row *count* + caveat surface before this was safe) proved unnecessary: a skipped row is *provably not a transaction*, so nothing that could be a transaction is ever dropped and AD-12's "never show fewer rows than parsed" holds with no caveat. Crucially, a row that carries a valid date **or** an amount but can't fully parse still raises a typed `IngestionError` (`MISSING_AMOUNT` / `UNPARSEABLE_DATE`) -- honest refusal preserved. New non-raising `_is_valid_date` helper; regression tests + `sbi_with_footer.csv` fixture; full suite 105 passed. Source: inline adversarial code review (Edge Case Hunter lens), branch `epic-2-statement-upload-ingestion`.

## Deferred from: Story 2.3 implementation (2026-07-10)

- **Story 2.3 golden-file test deferred -- no real HDFC demo PDF fixture in the repo.** The AC "parse the demo HDFC PDF to exactly 24 rows matching `data/demo-data.json`" cannot be satisfied yet: there is no text-based HDFC PDF fixture anywhere in the repo, `data/demo-data.json` does not exist (only `prototypes/01-priyas-first-honest-morning-Prototype/data/demo-data.json` -- 24 tx in prototype shape `merchant`/`type`, not canonical `description_raw`/`direction`), and `reportlab` is not installed to synthesize one. **Built now (user decision 2026-07-10, "build logic now, defer golden test"):** the full `PDFParser` chain (statementsparser -> pdfplumber -> camelot, next tried only if previous yields no rows), the `ScannedPDFError` honest refusal for image PDFs (exact approved copy, code `NO_TEXT_LAYER`), and the shared `map_table` reuse of the CSV column mapping -- all unit-tested via injected/mocked extractors + a mocked `pdfplumber` + a mocked `statementparser.parse` (full suite 114 passed). **Deferred:** an end-to-end golden test that runs a real text-based HDFC demo PDF through the chain and asserts exactly 24 canonical rows -- this is also what validates the three adapters' fidelity against a real HDFC layout (statementsparser's HDFC path firing; pdfplumber/camelot header/column detection on real tables). **Unblock by:** adding a real text-based HDFC demo PDF to `tests/ingestion/fixtures/` and materializing `data/demo-data.json` as the expected-output oracle (or installing `reportlab` to synthesize a representative statement PDF). Owner: Story 2.3 follow-up / folds naturally into Story 8.5 (demo dry-run regression). Source: Story 2.3 implementation, branch `epic-2-statement-upload-ingestion`.

## Deferred from: Story 2.4 implementation (2026-07-10)

- **Full upload-flow browser E2E not run -- `reflex run` cannot boot in this environment.** Story 2.4 was verified by `reflex compile --dry` (SUCCESS -- the upload page + state compile) and unit tests (121 passed, incl. the new `parse_statement` dispatch tests), but the live prod server fails at Reflex's `check_schema_up_to_date()`: `ModuleNotFoundError: No module named 'psycopg2'` against the default local Postgres DSN. A SQLite fallback boot also failed (`unable to open database file` -- the project path contains a space, "BMAD ALPHA", which trips Windows sqlite-URL quoting). Both are **environment/DB-config issues orthogonal to the 2.4 code**. To unblock a live run: `pip install psycopg2-binary` + bring up the compose `db` service (or fix the SQLite DSN quoting for a driver-free smoke boot). The full register->upload->parse->summary->CTA browser E2E (and the `rx.State` async-handler assertions generally) folds into Story 8.5 / the Epic 8 E2E, consistent with the existing deferral of `rx.State` handler E2E to Epic 8. **UPDATE 2026-07-10 (final Epic-2 sweep): boot blocker resolved.** `psycopg2-binary` (already pinned in requirements.txt) was installed into the `.venv` (it was a venv/requirements drift), and the app now **boots via `reflex run --env prod`** using a relative SQLite DSN (`sqlite:///data/verify.db`, sidestepping the path-with-space) — verified serving `/` and `/upload` at **HTTP 200** with a full production build. What remains for Epic 8 is only the *interactive* browser E2E (driving an actual file upload -> progress -> summary -> persisted rows), which needs browser automation. The non-UI parse->persist seam is now covered end to end by `tests/ingestion/test_pipeline_integration.py`.
- **Upload progress steps 3 & 4 ("Categorising by Rules" / "AI Assist") are skeleton; the summary's rules/AI split is 0 until Epic 3.** By design (S2.4 DoD note): 2.4 wires the *real* parse so the transaction *total* is real, but categorization is Epic 3 (S3.1/S3.2). Until then every parsed row counts as "needs your help" and steps 3/4 fire as placeholder events. Not a defect -- tracked so Epic 3 knows to replace the skeleton counts. `finance_app/state/upload_state.py` marks the integration point.
- ~~**Parsed transactions are not yet persisted.** 2.4 parses + reports the honest count; dedup + persistence (with `user_id`/`source_file_id`) is Story 2.5. The parsed rows are currently discarded after counting -- 2.5 will persist them (and may fold the persist into this same handler to avoid a re-parse).~~ **✅ RESOLVED 2026-07-10** (Story 2.5): `services/ingestion/persist.py` (`persist_transactions` -- model-injected per AD-2, user-scoped dedup per AD-4 on the canonical key) is now wired into `upload_state._run_parse`, so uploads persist deduped rows and a re-upload / overlapping range inserts only genuinely-new ones. Covered by `tests/ingestion/test_dedup.py` (full suite 130 passed). The live browser round-trip remains part of the Epic-8 E2E (reflex-run env blocker).

## Deferred from: code review of story-3.2 (2026-07-10)

- **True per-row partial credit within one Tier-2 batch call** [services/categorize/llm_categorizer.py] — the strict `Literal`-typed structured-output response (AD-7's hard constraint) is validated as one JSON document; one bad item still fails the whole parse even after this review's patches (which reduce how often that happens via right-sized `max_tokens` and make the failure clean/tested, but don't eliminate it). True isolation would need a different response shape (per-item tool calls, or a lenient/partial parse with per-item fallback) — bigger than a patch; revisit once real usage shows how often it matters.
- **No UI disclosure that transaction text is sent to Claude for categorization** [product/UI] — NFR-5 already approves sending transaction text to Claude for categorization/Q&A; the "UI states this boundary" half of that NFR has no home yet (no settings/privacy-notice screen exists). Track for a future UI/Epic-8 story.
- **Refusal/network/auth/validation failures all logged identically** [services/categorize/llm_categorizer.py] — distinguishing `stop_reason="refusal"` from a network error, auth failure, or validation error would aid production diagnostics (e.g. a broken API key vs. an actual prompt-injection attempt) but isn't required by any AC. Observability polish for a future hardening pass.
- **No explicit client-side timeout on the categorization call** [services/categorize/llm_categorizer.py] — the Anthropic SDK computes a bounded default timeout from `max_tokens` already (not unbounded). An explicit tighter `asyncio.wait_for` deadline is reasonable future UX polish, not a correctness gap.

## Deferred from: code review of story-3.1 (2026-07-10)

- **`direction` equality uses a loose string match with no DB-level constraint** [finance_app/state/transactions_state.py] — pre-existing pattern from Story 1.2 (`direction` is a plain `str` column app-wide); not introduced or worsened by this diff, and the canonical `Transaction` dataclass already validates `direction` is a real `Direction` enum before persist.
- **Reserved chip keys `"all"`/`"needs_review"` could collide with a future category value** [finance_app/state/transactions_state.py] — theoretical; no category in the current, curated `CATEGORIES` taxonomy matches either literal.
- **Stale `active_filter` after a data change can leave no chip highlighted** [finance_app/state/transactions_state.py] — low-probability timing edge case; user can just click "All" again.
- **`category == Uncategorized` with `confidence >= threshold` is a theoretical inconsistency** [finance_app/state/transactions_state.py] — not reachable via the current `rules.py` engine (it always pairs `Uncategorized` with `confidence 0.0`).
- **Negative debit amounts would render with a bare `-` sign, no anomaly flag** [finance_app/state/transactions_state.py] — not reachable via the current ingestion path (parsers always store `abs(amount)` per AD-8).
- **No pagination/limit on `load_user_transaction_rows`** [finance_app/state/transactions_state.py] — per project-context's Performance Rules ("not as speculative gold-plating on a single-user local MVP"); revisit if/when transaction volume actually grows.
- ~~**`NEEDS_REVIEW_THRESHOLD = 1.0` will need revisiting once Tier-2 returns graded confidences** [finance_app/state/transactions_state.py] — already explicitly scoped to Story 3.4 in Story 3.1's own Dev Notes ("Full confidence-threshold badge logic ... → Story 3.4").~~ **✅ RESOLVED 2026-07-10** (Story 3.4): the threshold itself stayed `1.0`, unchanged — Tier-2's real, LLM-self-reported `category_confidence` never gates the badge decision at all now. `_to_row` computes `is_ai = category_source == 'llm' and category != UNCATEGORIZED` and checks it *before* the confidence threshold; an `llm`-sourced row is never subject to `NEEDS_REVIEW_THRESHOLD`. No longer deferred.

## Deferred from: code review of story-3.4 (2026-07-10)

- **The "full confidence = 1.0" threshold/constant is duplicated across four locations with no shared source of truth.** `NEEDS_REVIEW_THRESHOLD = 1.0` [finance_app/state/transactions_state.py], `FULL_CONFIDENCE = 1.0` [services/categorize/summary.py — whose own docstring says it "must agree with" the transactions table], the hardcoded `1.0` in `TransactionsState.save_correction`'s single-row branch, and the hardcoded `1.0` in `services/categorize/teach_me.py::reapply_correction`. Story 3.4 fixed the transactions-table copy of the "what counts as fully confident" logic but didn't consolidate the duplication pattern — pre-existing across Stories 3.1-3.3, not introduced by 3.4. A future change to one of these four copies could silently desync the upload-summary counts from the transactions-table badges with nothing to catch it. Consolidate into one shared constant (e.g. in `services/categorize/schema.py` alongside `CATEGORIES`) when next touching any of these four files.
- **`category_source` values outside the 3-member `CategorySource` enum (or `None`) are silently treated as non-AI with no anomaly logging.** [finance_app/state/transactions_state.py's `_to_row`] `category_source` is a plain nullable `str` DB column with no CHECK constraint — only application code (consistently, today) writes one of `rule`/`llm`/`user`. A corrupted/legacy/case-variant value (`None`, `"LLM"`, stray whitespace) is silently folded into the "not AI" branch, unlike this same file's existing practice of logging a warning for other malformed-row anomalies (`load_user_transaction_rows`'s `except ValueError`). No current write path produces this, so not blocking — add a `log.warning` if this ever needs diagnosing.
- **`category_confidence > 1.0` on a non-`llm`-sourced row silently produces "no badge" instead of surfacing the corruption.** [finance_app/state/transactions_state.py's `_to_row`] `category_confidence` has no DB-level bounds constraint (only `CategorizationResult.confidence`'s `Field(ge=0.0, le=1.0)` in `services/categorize/llm_categorizer.py` constrains the Tier-2 write path, not arbitrary DB state). A legacy/corrupted value like `1.5` evaluates `1.5 < 1.0 → False`, identical output to a legitimate full-confidence row. No current write path produces this; same low-priority observability gap as the item above.

## Deferred from: story-3.4 planning — virtual scroll (2026-07-10)

- **Transaction list virtual scrolling — a real future improvement, not an abandoned requirement.** epics.md's Story 3.4 AC explicitly requires virtual scroll for the transaction list ("standard DOM list is not acceptable for 100–300 rows... renders without layout thrash at 300 rows"). Two independent project artifacts already questioned this before Story 3.4 was drafted: the WDS prototype (`01.4-transactions-table.html`) renders the full list with no virtualization at all, and the project's own UX-scenario notes for this screen flagged it as "originally justified by budget Android device performance... on a desktop-only MVP, a standard DOM list will likely render fine... flagged for review, not removed." **User decision (2026-07-10):** skip virtual scroll for Story 3.4 — implement it as a plain list, matching the WDS prototype — but revisit as a genuine improvement once all epics are built, not drop it permanently. Reflex has no built-in virtualized list; implementing this for real means wrapping an external React library (e.g. `react-window`/`@tanstack/react-virtual`) as a custom Reflex component — new architectural ground for this codebase. Revisit at the end of the epic roadmap (natural fit: alongside or after Epic 8 hardening), once real transaction-count/performance data from actual use exists to justify (or rule out) the investment.

## Deferred from: code review of story-3.3 (2026-07-10)

- **Taught rule pattern is the full raw description, not a normalized merchant name** [services/categorize/teach_me.py, finance_app/state/transactions_state.py] — already a documented Dev Notes scope decision (no merchant-name normalization exists in this codebase yet); user re-confirmed 2026-07-10 to leave as-is. All three review layers independently flagged that real bank description text carries unique reference numbers/dates, so a rule taught on one transaction will likely rarely match another transaction or a future upload — this undercuts "apply to all matching transactions" and "remember for future uploads" without any error surfaced to the user. Fold merchant-name extraction/normalization into a future story.
- **No DB-level unique constraint on `(user_id, pattern)` in `MerchantRule`** [finance_app/models.py] — pre-existing table shape from Story 1.2; needs a migration. The code-review pass made `write_merchant_rule`'s upsert lookup case-insensitive, which closes the case-drift duplication path, but a true concurrent double-submit (double-click "save", or two browser tabs correcting the same merchant near-simultaneously) can still both see "not found" and insert two rows for the same pattern — the check-then-write is application-level only, with no DB constraint as a backstop. Add a unique index + catch the resulting `IntegrityError` gracefully (AD-12) in a future hardening pass (Epic 8).

## Deferred from: code review of Story 2.4 (2026-07-10)

- ~~**CSVParser refuses non-UTF-8 CSVs instead of parsing them (low, UX).** After the 2.4 review fix, a non-UTF-8 bank CSV (e.g. cp1252 / latin-1, which some Indian banks export) now gets a clean typed refusal (`PARSE_FAILED`) rather than an untyped `UnicodeDecodeError` crash -- but it is still a *refusal*. Better UX: `CSVParser` could open `utf-8-sig` and fall back to `cp1252` / `latin-1` on `UnicodeDecodeError` so those statements actually parse. Small, self-contained; folds into Story 8.1 (edge-case pass) or a 2.2 hardening follow-up.~~ **✅ RESOLVED 2026-07-10** (user request -- fixed now instead of deferring): `CSVParser.parse` now reads via a new `_read_csv_text` helper that tries `utf-8-sig` -> `cp1252` -> `latin-1`, so non-UTF-8 bank exports **parse** rather than being refused (latin-1 maps every byte, so an untyped `UnicodeDecodeError` can no longer occur). Regression test `test_non_utf8_cp1252_csv_parses`; the dispatch-level `PARSE_FAILED` wrap stays as a safety net for other unexpected errors (csv.Error, OSError). Full suite 123 passed. Source: inline adversarial code review of Story 2.4, branch `epic-2-statement-upload-ingestion`.
## Discovered AND fixed while implementing Story 7.2 (2026-07-10/11, user request)

- ~~**`services/narrate/tools.py` imports `finance_app.models.Transaction`, violating AD-2 and failing `tests/test_service_boundary.py::test_no_ui_layer_import_in_services`.**~~ **✅ FIXED 2026-07-11** (ALPHA explicitly asked for this to be fixed once surfaced, rather than left deferred). Introduced in commit `183a7fc` ("fix(epic-6): address 8 code-review findings in Copilot service layer", 2026-07-10, author `panchadir`). **First attempt (superseded):** relocated `Transaction` into a new `services/data/models.py` (plain `sqlmodel.SQLModel`, zero `reflex` import), re-exported from `finance_app.models`. This worked (verified identity-equal, zero regressions) but a code-review pass (Blind Hunter adversarial review of this same story) correctly flagged it as a bigger change than necessary: `services/ingestion/persist.py` (Story 2.5) had *already* solved this exact problem — "a `services/` module needs the `Transaction` ORM class without importing `finance_app`" — via dependency injection (`persist_transactions(session, txn_model: type, ...)`, the model class passed in by the caller, never imported). **Final fix:** reverted the relocation entirely (deleted `services/data/`, restored `finance_app/models.py`'s original inline `Transaction` class unchanged) and instead threaded `txn_model: type` as an injected parameter through `services/narrate/tools.py` (`run_tool`, `_query_transactions`, `_get_spending_by_category`) → `services/narrate/copilot.py` (`astream_events`) → `finance_app/state/copilot_state.py` (passes `Transaction` from `finance_app.models`, mirroring exactly how it already passes `session_factory=rx.session`). Net effect: `Transaction` has exactly one home (`finance_app.models`, unchanged), no FK-string duplication, no new package — the established DI pattern reused instead of a second, competing solution to the same problem. Added `tests/narrate/test_tools.py` (5 tests: user-scoping, category/direction filtering, the 50-row limit cap, category-sum aggregation, calling `run_tool(..., txn_model=Transaction)`) since `services/narrate/tools.py` had **zero** test coverage before this session. Verified: `pytest tests/` (entire repo) → **432 passed, 6 skipped**, zero failures. `test_no_ui_layer_import_in_services` passes with zero offenders.

## Fixed pre-existing bug found while starting Story 7.2 (2026-07-10)

- ~~**`services/narrate/config.py` had an unterminated triple-quoted string, a `SyntaxError` that broke the entire `services/narrate` package import.**~~ **✅ FIXED 2026-07-10.** The merge commit `22223b1` ("Epic-6 changes") concatenated the Epic-5 version of `config.py` (docstring + `CATEGORIZATION_MODEL`/`NARRATION_MODEL`/`COST_LEVER_MODEL`/`BRIEFING_MAX_TOKENS`/`NARRATION_TEMPERATURE`/`API_KEY_ENV_VAR`, used by `briefing.py`) with Story 6.1's version (a second docstring + `CATEGORIZE_MODEL`/`COPILOT_MODEL`/`COPILOT_MODEL_COST_LEVER`, used by `copilot.py`) without resolving the conflict — a stray `"""` at line 39 opened a string that ran to EOF. `import services.narrate` (and therefore anything importing the Copilot or Dashboard-briefing state) failed immediately on a fresh build; the bug was invisible in the currently-running dev container only because that container's image predates Epic 5/6 (stale `docker cp`-overlaid state, not a fresh build). **Fix:** merged the two blocks, keeping every constant actually imported somewhere (`briefing.py` needs `NARRATION_MODEL`/`BRIEFING_MAX_TOKENS`/`NARRATION_TEMPERATURE`/`API_KEY_ENV_VAR`; `copilot.py` needs `COPILOT_MODEL`) and dropping the two genuinely-unused duplicates (`CATEGORIZE_MODEL`, `COPILOT_MODEL_COST_LEVER` — confirmed via repo-wide grep, nothing imports them). Verified: `ast.parse` clean, both `briefing.py`/`copilot.py` import, `pytest tests/narrate/` → 24 passed, `pytest tests/engine/ tests/test_service_boundary.py` → 244 passed/6 skipped (no regression). Surfaced because Story 7.2 (`services/narrate/insight_narrator.py`) would have hit this same `SyntaxError` on its first import. Discovered and fixed before writing the Story 7.2 story file, not part of Story 7.2's own scope.

## Deferred from: code review of story-7.1 (2026-07-10)

- **`_latest_balance` has no staleness check relative to `ctx.as_of`, and same-day multiple balance-bearing transactions resolve arbitrarily via `max()`** (`services/engine/insights/detectors.py:100`). Pre-existing date-only-granularity limitation shared with the Epic 4 engine; not introduced by this diff.
- **Overdue commitments (`due_date < ctx.as_of`) never trigger `UpcomingCommitmentCollisionDetector`** (`services/engine/insights/detectors.py:277`). Mirrors the already-logged Epic-4 item on overdue *predicted* commitments not being reserved by the STS engine — same root question ("does an overdue-but-unconfirmed-paid item still get treated as owed?"), one place to resolve it for both the engine and the insight detector.
- **`ZombieSubscriptionDetector` requires every consecutive gap to fall in `[20, 40]` days, with no tolerance for a single outlier cycle** (`services/engine/insights/detectors.py:206`). A delayed charge, a skipped/refunded month, or billing-date drift near a 31-day month disqualifies an otherwise-clear recurring subscription entirely. Threshold-tuning design call, not MVP-blocking.
- **`DeathBySmallPurchasesDetector` has no time-spread requirement** (`services/engine/insights/detectors.py:160`). A single-day burst of small purchases (e.g. one shopping trip) fires identically to a genuine month-long drip. KISS default acceptable for MVP; revisit if it produces demo noise.
- **Evidence-point selection ordering is inconsistent across detectors** — amount-descending in `_evidence()` (used by Post-payday, Death-by-small, Zombie, Weekend) vs. due-date order in the collision detector (`services/engine/insights/detectors.py:73`, `:290`). Deferred to Story 7.2, which owns narrative coherence of cited evidence order once it writes the O→E→E→A prose.

## Deferred from: code review of Epic 4 engine (stories 4-2/4-3/4-4) (2026-07-10)

- ~~**After-income layer double-counts current balance** (`services/engine/safe_to_spend.py:180`, `_after_income_layer`). The after-income per-day figure spreads `available_balance + income − next-cycle-reserved − buffer` over 30 days, but the *today* layer already draws spendable from that same `available_balance` — the balance is counted in both layers. This is the source of the scenario-3 gap (engine ₹1,830 vs the scenario file's flagged-approximate ~₹990). Already recorded as contract §8 open item and in story 4-3; carried here for the ledger. **Spec-owner decision pending:** correct the scenario-file figure, or refine the after-income model (e.g. exclude the rolled-over balance, or spread only income over the next cycle).~~ **✅ RESOLVED 2026-07-10** (Epic 5, owner-approved model choice): `_after_income_layer` now spreads **the income only** — the rolled-over `available_balance` is no longer added back, because today's layer is already spending it down over `days_to_income`. A shortfall (`spendable_pool < 0`) **carries forward** and is subtracted from the incoming salary; a surplus does not carry (it was already offered as today's spend). This closes contract §8: scenario 3's after-income layer is now exact (₹55,000 ÷ 30 = ₹1,833.33 → **₹1,830**), and the file's flagged-approximate "~₹990" is superseded (it never matched any stated input). **Second defect found while fixing it:** 10 of the 13 scenarios supply a `next_income_date` with **no** `next_income_amount`, so the old model produced an "after your salary" figure derived *entirely from the balance* — a number that never touched a salary (NFR-1 violation). Those now return `None` plus a new `income_amount_unknown` data-quality flag, and the Dashboard says which piece is missing rather than claiming no salary was detected. Verified end to end: the card previously read "you're ₹5,460 short today" above "₹3,600/day after payday"; it now reads ₹0 today / **₹2,650/day** after. Tests: 5 new in `tests/engine/test_scenarios.py`; suite 424 passed.
- ~~**`safety_ok=True` while Safe-to-Spend=₹0 when the balance covers commitments but dents the buffer** (`services/engine/safe_to_spend.py:252`). `safety_ok = available_balance >= reserved_total` ignores the buffer, so `reserved ≤ balance < reserved+buffer` yields `pool<0 → STS ₹0` while `safety_ok` stays True (Confidence Score lands ~40). Untested boundary; mild CS-4 tension. Decide whether the emergency buffer belongs inside `safety_ok` or document the commitment-safety-vs-buffer-health split explicitly. Low severity (no scenario hits it; single-user MVP).~~ **✅ RESOLVED 2026-07-10** (Epic 5): the split is now explicit rather than documented-away. `safety_ok` keeps its meaning — *can every ring-fenced commitment be paid?* — and a new `buffer_intact` field answers *is the emergency buffer whole?* The Confidence Score bands the three states strictly: shortfall `[0,20]` < buffer-dented `[20,39]` < covered `[40,95]`, so a ₹0 Safe-to-Spend can **never** present as "On track" (this boundary previously scored exactly 40). **Third defect found while fixing it:** the shortfall driver read `-spendable_pool`, which includes the buffer — so with a ₹16,000 balance against ₹23,500 of bills it claimed the bills "exceed your balance by ₹9,500" when they exceed it by **₹7,500**. The driver now names `reserved_total − available_balance`; `-spendable_pool` remains the Confidence Score's `suggested_action` target ("free up ₹9,500" also restores the buffer). The buffer-dented state gets its own honest driver and its own hero-card line. Untested boundary is now a 6-test class; `EvidencePack` gained `buffer_intact` and `buffer`. Contract §4 amended.
- **Overdue predicted commitment is surfaced, not reserved** (`services/engine/safe_to_spend.py:148`). When a *predicted* commitment's `due_date` is before `as_of` (`days_until_due < 0`), it falls to the "outside window" branch and is not ring-fenced — even a *critical* one due before payday. A *known* overdue commitment is reserved. Defensible (a predicted/overdue item may already be paid), but it brushes the "safety beats precision" principle. Revisit alongside the recurring-debit auto-detector (Epic 5 / S7.1), which is what produces predicted commitments.

## Surfaced during Stories 5.4/5.6, then fixed — Epic 6 (Copilot) breakage (2026-07-11)

Discovered while verifying 5.4/5.6 (both files were untouched by that work) — the "Epic 6 Copilot was scaffolded early, before its dependencies, and never finished" gap, now **resolved** at user request in the same session:

- ~~**`finance_app/pages/copilot.py` breaks `reflex compile`.** `rx.foreach(CopilotState.messages, …)` iterates `messages: list[dict]` — an untyped var Reflex 0.9.6 refuses to foreach (`UntypedVarError`). The whole-app compile aborts on the `copilot` page.~~ **✅ RESOLVED 2026-07-11:** `messages` is now typed `list[ChatMessageView]` (a dataclass, like the other pages' view models); `copilot.py` bubbles use attribute access (`msg.content` / `msg.role` / `msg.trace_sources`) and `load_history` / `send_message` build `ChatMessageView`s. **Whole-app `reflex compile` now succeeds** (34/33 components, copilot included).
- ~~**`services/narrate/tools.py` fails the AD-2 boundary test.** `tools.py` does `from finance_app.models import Transaction` at module level (a services/ module importing the UI layer); `test_no_ui_layer_import_in_services` is red.~~ **✅ RESOLVED 2026-07-11:** `tools.py` is now pure — it holds only `TOOL_SCHEMAS` + a `CopilotData` `Protocol` + a `run_tool(name, input, *, data)` dispatcher, with **no `finance_app`/DB import**. The real reads moved to the new UI-layer `finance_app/state/copilot_data.py` (`DbCopilotData`, injected via `astream_events(..., data_factory=…)`). **Bonus — the three Epic-4/Epic-5 stubs are now wired:** `get_safe_to_spend` / `get_confidence_score` / `get_upcoming_commitments` return real data through the same `engine_bridge` the Dashboard uses (verified at runtime: STS ₹2,800 reserving a ₹20,000 commitment, Confidence *label* only — raw score never leaves the server per FR-5.5, real category/commitment lists — all `formatINR`'d). Boundary test green; +5 pure dispatch tests (`tests/narrate/test_copilot_tools.py`). **Full suite: 581 passed, 6 skipped** — zero failures.

## Deferred from: Epic 5 implementation — stories 5.1/5.2/5.3/5.5 (2026-07-10)

- ~~**After-income layer is inert to commitments (observed, not introduced).** Driving the real flow on `epic-5-dashboard-commitments`, the hero card's second layer stayed at **₹3,600/day** across three states: statement-only, +₹8,500 EMI, and +₹20,000 rent (which pushed today's figure to ₹0 with `safety_ok=False`). Cause is the already-logged Epic-4 item above: `_after_income_layer` only subtracts commitments due *after* `next_income_date`, and both test commitments fell *before* payday, so neither reduced the after-income figure — while the rolled-over `available_balance` is counted in both layers. The user therefore sees "you're ₹5,460 short today" beside "₹3,600/day after payday", which reads as contradictory. **Not fixed in Epic 5** (the fix belongs in `services/engine/safe_to_spend.py`, owned by the Epic 4 dev, and the spec-owner decision on the model is still pending). Epic 5 renders whatever the engine returns — no compensating arithmetic in the UI (AD-1). **Unblock by:** resolving the contract §8 open item; the Dashboard will then be correct with no UI change.~~ **✅ RESOLVED 2026-07-10** (same session, at user request — see the Epic-4 entry above for the full resolution). Re-drove the identical flow afterwards: ₹0 today / ₹2,650/day after payday, and the shortfall driver names ₹3,460 (28,500 − 25,040) rather than the ₹5,460 pool gap. No longer deferred.
- ~~**Story 5.4 (charts) and Story 5.6 (commitment auto-detection) not built.** 5.4 reads `transactions.category`, which Epic 3 is still populating — charts today would render a single "Uncategorised" slice. 5.6 (recurring-debit detector producing *predicted* commitments) is P1 and interacts with the "overdue predicted commitment is surfaced, not reserved" item above. Both are scoped out of this branch by agreement, not forgotten. The Dashboard's below-fold grid already has the slot.~~ **✅ RESOLVED 2026-07-11** (user request, on branch `Epic-7-insights` — Epic 3 categorization has since landed, removing 5.4's blocker). **Story 5.4:** pure debit-only aggregation in new `services/analytics/spending.py` (`spending_by_category` / `monthly_spend`, Decimal, 7 unit tests) feeds two below-the-fold `rx.plotly` figures built in `dashboard_state` — a category donut and a monthly-pace bar — plus an upcoming-commitments timeline (name/amount/next-due/tier) reusing the WDS `.timeline` markup. Every rupee shown is `formatINR`'d: donut hover and the pace y-axis **ticktext** are explicit `formatINR` strings (verified at runtime: ticks `₹0…₹10,000`, hover `₹25,500`), so no raw `125000.0` reaches a label (AC met). Credits never net against spend (tested). Charts sit after the hero+briefing and are hidden when there are no debits (UX-DR1). **Story 5.6:** pure `services/engine/commitment_detector.py` (`detect_recurring_commitments`: ±10% amount, 25–35-day cadence, ≥2 occurrences → `CommitmentCandidate` with signature/median-amount/modal-due-day; 11 unit tests incl. the three AC scenarios) surfaced on the Commitments page as "We noticed these recurring charges" confirm/dismiss prompts. Decisions persist in a new `commitment_suggestions` table (model + alembic `a7c1e9d4b2f0`) so a dismissed **or** confirmed signature is never re-proposed (`detect_commitment_candidates` excludes them under the `user_id` filter — 3 new bridge tests incl. cross-user isolation). Confirm creates a normal `Commitment` row and recomputes STS exactly like a manual add (feeds ring-fencing immediately). App **compiles clean** (`reflex compile` success) for both pages; full suite **575 passed** (+19 new: 9 detector, 7 analytics, 3 bridge). **Still deferred:** the interactive browser round-trip for the new `rx.State` handlers (`confirm_suggestion`/`dismiss_suggestion` and the chart on_load) — same `rx.State`-harness gap as the item below; folds into Story 8.5. Note 5.6 creates **user-confirmed** commitments (`is_predicted=False`), so it does *not* interact with the "overdue predicted commitment" item — that item still awaits a future predicted-commitment source.
- **`rx.State` handlers for the two new pages are unit-untested; the browser round-trip is not driven.** `DashboardState.load_dashboard` and every `CommitmentsState` mutation are verified by (a) the pure functions beneath them — `engine_bridge` (32 tests, real SQLite, incl. the atomic score writeback and cross-user isolation) and `validate_commitment` (32 tests) — plus (b) `reflex compile --dry` clean and (c) a live `reflex run --env prod` boot where `/dashboard` and `/commitments` both serve **HTTP 200** with the WDS markup. What is *not* covered: clicking Add → modal → save → seeing the impact bar update in a real browser. Same `rx.State`-harness gap already deferred for Epics 1 and 2; folds into the Epic 8 / Story 8.5 E2E.
- **`recent_score_events` orders by `(timestamp DESC, id DESC)` because `timestamp` has second-ish resolution.** Three score events written inside one request all render as "just now" and can share a timestamp; the `id` tiebreak keeps the drill-in panel in true insertion order. If the panel ever needs sub-second ordering guarantees, make `timestamp` the sortable key at write time rather than relying on the surrogate key.
- ~~**Naive/aware timestamp convention pinned, not fixed at the source.** `models._utcnow()` still writes tz-aware UTC into naive `DateTime` columns (logged in the Story 1.2 review above, which asked that this be standardized "when timestamps are first consumed (… Epic 5 dashboard freshness)"). Epic 5 is that consumer. Rather than migrate the columns mid-epic, `engine_bridge._as_utc` pins the read-side convention — **naive means UTC** — so `humanize_since` cannot raise on a naive/aware comparison (regression-tested). The column-level fix is still owed.~~ **✅ RESOLVED 2026-07-10** (same session, at user request): `models._utcnow()` fixed at the source — see the Story 1.2 entry above. The read-side `_as_utc` lift is retained deliberately, not as a workaround. No longer deferred.
- **Per-row Edit/Delete replaces the prototype's "…" popover (deliberate divergence, WDS 02.1).** A per-row dropdown needs per-row open state in `rx.State` and buys nothing for two actions; both buttons are now inline, keyboard-focusable, and `aria-label`led. New `.commit-row-action` CSS in `assets/wds.css` (the existing `.commit-menu-btn` is a 32px icon circle that would clip the text, and `.danger` was scoped to `.commit-menu-dropdown button`). Revisit if the row grows a third action.

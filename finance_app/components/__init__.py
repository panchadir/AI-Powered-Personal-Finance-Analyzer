"""Reusable Reflex components, mirroring the pages they serve.

Convention: a page ``pages/dashboard.py`` is served by components under
``components/dashboard/`` (or a shared helper here). Populated per epic.
"""
